@*partial view no es una página completa, es como un widget que podemos usar en diferentes Views, Esta no es una vista parcial*@

@*Se agrega para poder ver las propiedades del modelo*@
@model Vidly.ViewModels.RandomMovieViewModel 
@{
    ViewBag.Title = "Random";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*
    Si el className es null entonces el class no se renderea en el html
    En caso contrario se le asigna el class popular
    Esto sirve para renderear clases o atributos condicionalmente
*@

@{
    var className = Model.Customers.Count > 5 ? "popular" : null;
}

<h2 class="@className">@Model.Movie.Name</h2>

@if (Model.Customers.Count == 0)
{
    <text> No one has rented this movie before </text>
}
else
{
    <ul>
        @foreach (var customer in Model.Customers)
        {
            <li>@customer.Name</li>
        }
    </ul>
}

